/**
 ****************************************************************************************************
 * @file        game2048.h
 * @author      (本项目: 游戏机架构)
 * @version     V1.0
 * @date        2026-09-25
 * @brief       2048 游戏 —— 实现统一游戏接口(game_api)
 *
 *              核心机制:
 *                - 4x4 格子, 上下左右滑动合并相同数字
 *                - 目标是合成 2048, 每次滑动随机生成 2 或 4
 *                - 最高分通过 save 模块持久化到 SD 卡 /SAVE/game2048.sav
 *
 *              按键映射(USB 键盘 + 板载按键, 由 input 层统一转换):
 *                ↑↓←→ 滑动, ESC = 退出回菜单
 ****************************************************************************************************
 */

#ifndef __GAME2048_H
#define __GAME2048_H

#include "./SYSTEM/sys/sys.h"
#include "./BSP/LCD/lcd.h"
#include "./game_api.h"

/* 获取 2048 的 game_t 实例 */
const game_t *game2048_get_game(void);

/* 游戏是否仍在运行(0=已退出) */
uint8_t game2048_is_running(void);

#endif /* __GAME2048_H */
